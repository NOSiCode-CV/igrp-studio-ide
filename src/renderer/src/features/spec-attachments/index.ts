/**
 * Public API — chat-level spec/document attachment picker.
 *
 * Reused across the Specification generator panes (Documents chat,
 * Prototype build chat) and any future surface that wants the user to
 * pin docs to the current AI conversation.
 */

export { DocAttachPicker } from './DocAttachPicker'
