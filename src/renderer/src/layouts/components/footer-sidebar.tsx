import SupportContent from '@renderer/components/support-content';

const FooterSidebar = () => {

    return (
        <>
            <SupportContent />
        </>
    );
};

export default FooterSidebar;
